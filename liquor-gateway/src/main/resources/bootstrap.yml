server:
  port: 8001

spring:
  redis:
    host: ${redis.host}
    password: ${redis.pwd}
  application:
    name: liquor-gateway
  # 配置中心
#  cloud:
#    config:
#      fail-fast: true
#      name: ${spring.application.name}
#      profile: ${spring.profiles.active}
#      discovery:
#        enabled: true
#        service-id: liquor-config
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

# 注册中心
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: ${eureka.defaultZoneClient}


